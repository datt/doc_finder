<h2>Create Doctor Profile</h2>
<hr>
<%= simple_form_for([:admin, @doctor], :html => {:class => 'form-horizontal' }) do |f| %>
  <%= render :partial => 'form', :locals => { :f => f, :name => "Create" } %>
<%end%>